/** Тариф, за который произведена оплата. Это не `Plan` (тот описывает
 *  текущий тариф customer'а), а конкретная биллинговая позиция:
 *  месячная или годовая подписка. */
export enum PaidPlan {
  PRO_MONTHLY = 1,
  PRO_YEARLY = 2,
}
