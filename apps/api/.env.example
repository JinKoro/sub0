# Runtime
NODE_ENV=development

# HTTP server
API_PORT=3001

# Comma-separated list of origins allowed by CORS (no wildcards in prod)
CORS_ORIGINS=http://localhost:3000

# Strapi (server-only — never expose to the browser)
STRAPI_URL=
STRAPI_TOKEN=
